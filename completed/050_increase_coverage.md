# Task: Increase Coverage for PR #7087

PR #7087 (mojolicious-style signatures) shows incomplete coverage due to signature-induced opcode changes and missing exercise of default arguments.

## Plan

1.  **Exercise Signature Default Arguments**
    - Identify subroutines in `t/` and `lib/` with default values that are always passed by tests.
    - Add test cases that call these subroutines without the optional arguments.
    - Target: `t/full-stack.t` (`assign_jobs`), `t/05-scheduler-full.t` (`find_worker`), etc.

2.  **Fix `Devel::Cover` Attribution for Anonymous Subs**
    - Lines like `Mojo::IOLoop->recurring(...)` in `lib/OpenQA/Worker.pm` or `sub { ... }` assignments in `t/16-utils.t` are showing as missed.
    - Ensure these lines are either exercised by a test that doesn't mock the containing module too early, or mark them as `# uncoverable statement` if they are truly unreachable in the test environment (e.g. IPMI hardware logic).

3.  **Re-align `# uncoverable` Pragmas**
    - Audit all files where `# uncoverable` was added but failed to squash the miss (e.g. `t/30-test_parser.t`).
    - Use `uncoverable subroutine` for entire small subs if they are helpers for deparsing/mocking.
    - Adjust `count:N` to match the new opcode structure generated by signatures.

4.  **Verification**
    - Run `make tidy` to ensure formatting is correct.
    - Run `make test-unit-and-integration` for affected files.
    - Verify local coverage if possible, or rely on CI feedback.

## Implementation Details

### `t/full-stack.t`

- Add a call to `assign_jobs()` without arguments to cover the `$worker_class = undef` branch.

### `lib/OpenQA/Worker.pm`

- Mark the `Mojo::IOLoop->recurring` call as `# uncoverable statement` since it schedules a periodic task that is usually mocked in tests to avoid actual timers.

### `t/30-test_parser.t`

- Fix the `uncoverable` markers for `has result1 => sub { ... }`.

### `lib/OpenQA/WebSockets.pm`

- Ensure the retry logic in `ws_send` is marked correctly.
